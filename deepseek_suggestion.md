好，我已经全面阅读了这个项目。以下是我的分析：

---

## 🔍 项目概览

**EduGenesis** — 第十五届"中国软件杯" A3 赛题参赛作品（科大讯飞出题），目标是构建一个基于大模型的**个性化学习资源生成与多智能体学习系统**。

**技术栈：** FastAPI (Python) 后端 + React/Vite 前端 + SQLite + 讯飞星火大模型 + 讯飞 TTS

---

## ✅ 做得好的地方

1. **前后端分离清晰**，架构直观，本地一键可跑
2. **赛题对齐度高**：6维学生画像、多类型资源生成（PDF/幻灯片/测验/思维导图/代码）、学习路径规划，五项核心功能全部覆盖
3. **严格遵守竞赛规则**：使用讯飞星火大模型 + 讯飞 TTS，AI 工具选型符合出题企业要求
4. **流式输出**已实现（SSE），支持 Markdown 渲染和多模态卡片展示
5. **UI 投入大**：GSAP 动画、赛博朋克科幻风格、雷达图、SVG 脑图、typing effect 等视觉效果丰富
6. **安全风控意识**：代码沙盒有关键词过滤、超时熔断（2s）、临时文件隔离执行

---

## ⚠️ 核心问题与改进建议

### 1. 单文件巨大化 — 最紧迫的工程债务

| 文件                    | 大小    | 问题                                                         |
| :---------------------- | :------ | :----------------------------------------------------------- |
| `backend/app/routes.py` | ~117 KB | 所有路由、数据库操作、LLM 调用、TTS、沙盒、fallback 资源全部塞在一个文件 |
| `frontend/src/App.jsx`  | ~260 KB | 整个 SPA 的组件、状态、API 调用、CSS-in-JS 全在一个文件      |

**建议：** 这是比赛提交前最该做的事。后端按领域拆成 `auth.py`、`profile.py`、`resources.py`、`sandbox.py`、`chat.py`、`db.py`、`llm_client.py`；前端拆成独立组件文件（`Dashboard.jsx`、`ChatView.jsx`、`LearningPath.jsx`、`Sandbox.jsx` 等），引入一个轻量状态管理。

### 2. "多智能体"本质上是 Prompt Engineering，不是真正的 Multi-Agent

代码中四个智能体（主管/画像/路径/安全校验）的角色区分靠的是：
- 在 system prompt 里切换人格描述
- 大量**硬编码 fallback 内容**（`get_fallback_assets_for_topic()` 占了几百行）
- `db_log_agent_action()` 写日志时打上不同 sender 标签，**模拟**多智能体协作的"表象"

真正的多智能体应该是：各 Agent 有独立的状态/记忆/工具，之间通过消息传递或共享黑板协作。建议引入 **LangGraph** 或 **CrewAI** 来做真正的 Agent 编排，或者至少在文档中诚实说明当前实现方式。

### 3. 安全隐患（需立即修复）

- **密码哈希用裸 SHA-256**（无盐、无迭代）→ 应改用 `hashlib.pbkdf2_hmac` 或 `bcrypt`
- **`global logged_in_username`**（`routes.py:57`）— 全局单用户变量，并发访问下用户 A 会拿到用户 B 的数据。应改为从请求上下文（JWT token / session）中获取当前用户
- **沙盒黑名单可绕过**：`__import__('os')`、`getattr(__builtins__, 'ev' + 'al')` 等轻松绕过关键词过滤
- **TTS WebSocket 使用 `ssl.CERT_NONE`**（`routes.py:1828`）— 跳过证书验证

### 4. 缺少真正的知识库

赛题要求"自行构造至少一门完整高校专业课程的初始知识库"，目前只有：
- 两个硬编码的 8 节点学习路径（Python / 机器学习）
- 每个主题的 fallback 内容是固定模板套 `topic` 变量生成的

**没有真实的课程文档集、学术文献、知识点图谱**。建议至少准备一门课程的结构化知识库（Markdown 文件或向量数据库），让 LLM 基于真实素材生成资源而非凭空构造。

### 5. 多模态是"模拟"的

- "视频/动画"生成：`[VIDEO: ...]` 标签 → 前端展示一个带进度条的音频播放器 + CSS 动画，实际只是 TTS 朗读文字
- "思维导图"：硬编码的 SVG 坐标，不是动态生成的 Mermaid 渲染
- 赛题明确要求"多模态教学视频/动画"——这是核心得分点。建议至少集成一个真实的视频生成 API（如讯飞的 SeeDance 或开源方案）

### 6. 缺少测试

`requirements.txt` 没有 `pytest`（只在沙盒代码里引用），项目没有任何单元测试。评分中"配套文档丰富度"占 10%，其中包括测试说明书。

### 7. 缺少前端路由

`App.jsx` 用 `window.location.pathname` + `pushState` 手动解析 URL（`parseStateFromURL`），没有使用 React Router。这会导致浏览器后退/前进按钮行为异常、无法做路由守卫。

---

## 📊 总结评分（按赛题标准预估）

| 评分维度           | 占比 | 当前表现                                                  |
| :----------------- | :--- | :-------------------------------------------------------- |
| 创新价值与实用性   | 35%  | 中上 — 概念完整，但"多智能体"实现较浅                     |
| 功能实现及技术要求 | 45%  | 中 — 功能全但深度不够，视频/动画是模拟的                  |
| 配套文档丰富度     | 10%  | 未知 — docs/ 下有一些设计文档，但缺少系统说明书和测试文档 |
| 演示视频与PPT效果  | 10%  | 预计较好 — UI 视觉效果出色                                |

---

## 🎯 如果要冲刺优化，优先级建议

1. **拆分巨型文件**（工程基础，提交前必做）
2. **修复安全问题**（密码哈希 + 全局用户变量）
3. **接入真正的 Multi-Agent 框架** 或至少一个真实的视频生成（加分核心）
4. **构建真实知识库**（至少一门完整课程）
5. **引入前端路由**（React Router）
6. **写测试 + 配套文档**

这是一个"看起来很好"的原型 — 演示效果好，但技术深度和工程质量还有明显差距。如果时间充裕，建议把精力集中在让"多智能体"和"多模态生成"不再只是模拟，而是有真实技术支撑。